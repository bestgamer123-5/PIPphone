pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        gradlePluginPortal()
        mavenCentral()
    }
}

include 'carlib'
project(':carlib').projectDir = file('../CarLib')

rootProject.name = 'pipphone'
